<% provide(:title, "Sign in") %>
<h1>Sign in</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(:session, url: sessions_path) do |f| %>

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.submit "Sign in", class: "btn btn-large btn-primary" %>
    <%end %>
    
    <!-- alternative Form_for implemetation....you also need to change params[:session][:email] and params[:session][:password]
    to params[:email] and params[:password] for this to work.
      <%=form_tag('/sessions') do %>
      
      <%=label_tag 'email'%>
      <%=text_field_tag 'email'%>
    
      <%=label_tag 'password'%>
      <%=password_field_tag 'password'%>
      
      <%=button_tag 'submit',class: "btn btn-large btn-primary"  %>
      
    <%end%> -->

    <p>New user? <%= link_to "Sign up now!", signup_path %></p>
  </div>
</div>
